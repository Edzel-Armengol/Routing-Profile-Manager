node_modules/
build/
.aws-sam/
.env
.env.local
.env.*.local
!.env.example
npm-debug.log*

# Lambda deployment package — must be built from source, never committed
infrastructure/routing-profile-function.zip
