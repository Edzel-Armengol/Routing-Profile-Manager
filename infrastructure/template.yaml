AWSTemplateFormatVersion: "2010-09-09"
Transform: AWS::Serverless-2016-10-31
Description: Lambda Function URL backend for the Amazon Connect routing profile manager.

Parameters:
  LambdaCodeBucket:
    Type: String
    Description: S3 bucket in this Region containing the Lambda deployment ZIP.
  LambdaCodeKey:
    Type: String
    Default: routing-profile-function.zip
    Description: S3 object key for the Lambda deployment ZIP.
  ConnectInstanceId:
    Type: String
    Description: >
      Amazon Connect instance identifier (the UUID at the end of the instance ARN).
      Example: 5455ba30-b941-4270-a0bf-b3f57317121f
  ConnectInstanceArn:
    Type: String
    Description: >
      Full Amazon Connect instance ARN.
      Format: arn:aws:connect:<region>:<account-id>:instance/<ConnectInstanceId>
      Example: arn:aws:connect:ap-southeast-2:123456789012:instance/5455ba30-b941-4270-a0bf-b3f57317121f
  OktaIssuer:
    Type: String
    Description: >
      Okta tenant URL — provided by the client's Okta administrator.
      Must match the iss claim in tokens issued by the Okta tenant.
      Example: https://your-org.okta.com
  OktaClientId:
    Type: String
    Description: >
      Client ID of the Routing Profile Manager OIDC application in Okta.
      Provided by the client's Okta administrator after creating the OIDC app.
      Example: 0oa1b2c3d4e5f6g7h8i9
  AllowedRoutingProfiles:
    Type: String
    Default: ""
    Description: >
      Optional comma-separated list of routing profile names to expose to agents.
      When blank, all routing profiles in the Connect instance are available.
      Example: Basic Routing Profile,Premium Routing Profile
  FrontendBucketName:
    Type: String
    Description: >
      Name of the S3 bucket that hosts the React frontend build.
      Must be unique globally. Example: routing-profile-manager-frontend

Resources:

  # ---------------------------------------------------------------------------
  # S3 bucket for the React frontend (private — served via CloudFront only)
  # ---------------------------------------------------------------------------
  FrontendBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !Ref FrontendBucketName
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        BlockPublicPolicy: true
        IgnorePublicAcls: true
        RestrictPublicBuckets: true

  FrontendBucketPolicy:
    Type: AWS::S3::BucketPolicy
    Properties:
      Bucket: !Ref FrontendBucket
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: cloudfront.amazonaws.com
            Action: s3:GetObject
            Resource: !Sub ${FrontendBucket.Arn}/*
            Condition:
              StringEquals:
                AWS:SourceArn: !Sub arn:${AWS::Partition}:cloudfront::${AWS::AccountId}:distribution/${CloudFrontDistribution}

  # ---------------------------------------------------------------------------
  # CloudFront Origin Access Control (replaces legacy OAI)
  # ---------------------------------------------------------------------------
  FrontendOAC:
    Type: AWS::CloudFront::OriginAccessControl
    Properties:
      OriginAccessControlConfig:
        Name: !Sub ${AWS::StackName}-oac
        OriginAccessControlOriginType: s3
        SigningBehavior: always
        SigningProtocol: sigv4

  # ---------------------------------------------------------------------------
  # CloudFront Response Headers Policy — adds the CSP header recommended by
  # the Amazon Connect Agent Workspace Developer Guide so the app can only be
  # embedded inside the Connect agent workspace iframe.
  # ---------------------------------------------------------------------------
  CSPResponseHeadersPolicy:
    Type: AWS::CloudFront::ResponseHeadersPolicy
    Properties:
      ResponseHeadersPolicyConfig:
        Name: !Sub ${AWS::StackName}-csp-policy
        Comment: Restricts iframe embedding to Amazon Connect agent workspace only
        SecurityHeadersConfig:
          ContentSecurityPolicy:
            ContentSecurityPolicy: "frame-ancestors https://*.awsapps.com https://*.my.connect.aws"
            Override: true

  # ---------------------------------------------------------------------------
  # CloudFront distribution — serves the React frontend over HTTPS
  # ---------------------------------------------------------------------------
  CloudFrontDistribution:
    Type: AWS::CloudFront::Distribution
    Properties:
      DistributionConfig:
        Enabled: true
        DefaultRootObject: index.html
        Origins:
          - Id: S3Origin
            DomainName: !GetAtt FrontendBucket.RegionalDomainName
            OriginAccessControlId: !GetAtt FrontendOAC.Id
            S3OriginConfig:
              OriginAccessIdentity: ""
        DefaultCacheBehavior:
          TargetOriginId: S3Origin
          ViewerProtocolPolicy: redirect-to-https
          CachePolicyId: 658327ea-f89d-4fab-a63d-7e88639e58f6  # CachingOptimized (managed)
          ResponseHeadersPolicyId: !Ref CSPResponseHeadersPolicy
          AllowedMethods:
            - GET
            - HEAD
            - OPTIONS
          CachedMethods:
            - GET
            - HEAD
        # Return index.html for all routes (SPA routing support)
        CustomErrorResponses:
          - ErrorCode: 403
            ResponseCode: 200
            ResponsePagePath: /index.html
          - ErrorCode: 404
            ResponseCode: 200
            ResponsePagePath: /index.html
        HttpVersion: http2
        PriceClass: PriceClass_All

  # ---------------------------------------------------------------------------
  # Lambda function — handles routing profile list and update API calls
  # ---------------------------------------------------------------------------
  RoutingProfileFunction:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub ${AWS::StackName}-handler
      CodeUri:
        Bucket: !Ref LambdaCodeBucket
        Key: !Ref LambdaCodeKey
      Handler: routing_profile_manager.lambda_handler
      Runtime: python3.13
      Architectures:
        - arm64
      MemorySize: 128
      Timeout: 10
      Environment:
        Variables:
          CONNECT_INSTANCE_ID: !Ref ConnectInstanceId
          CONNECT_INSTANCE_ARN: !Ref ConnectInstanceArn
          OKTA_ISSUER: !Ref OktaIssuer
          OKTA_CLIENT_ID: !Ref OktaClientId
          ALLOWED_ROUTING_PROFILES: !Ref AllowedRoutingProfiles
          LOG_LEVEL: INFO
      Policies:
        - Version: "2012-10-17"
          Statement:
            - Effect: Allow
              Action: connect:ListRoutingProfiles
              Resource: !Sub arn:${AWS::Partition}:connect:${AWS::Region}:${AWS::AccountId}:instance/${ConnectInstanceId}
            - Effect: Allow
              Action: connect:UpdateUserRoutingProfile
              Resource:
                - !Sub arn:${AWS::Partition}:connect:${AWS::Region}:${AWS::AccountId}:instance/${ConnectInstanceId}/agent/*
                - !Sub arn:${AWS::Partition}:connect:${AWS::Region}:${AWS::AccountId}:instance/${ConnectInstanceId}/routing-profile/*

  RoutingProfileFunctionUrl:
    Type: AWS::Lambda::Url
    Properties:
      TargetFunctionArn: !GetAtt RoutingProfileFunction.Arn
      AuthType: NONE
      InvokeMode: BUFFERED
      Cors:
        AllowCredentials: false
        AllowHeaders:
          - Content-Type
          - Authorization
        AllowMethods:
          - GET
          - PUT
        AllowOrigins:
          - !Sub https://${CloudFrontDistribution.DomainName}
        MaxAge: 300

  RoutingProfileFunctionUrlPermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunctionUrl
      FunctionName: !Ref RoutingProfileFunction
      FunctionUrlAuthType: NONE
      Principal: "*"

  RoutingProfileFunctionInvokePermission:
    Type: AWS::Lambda::Permission
    Properties:
      Action: lambda:InvokeFunction
      FunctionName: !Ref RoutingProfileFunction
      InvokedViaFunctionUrl: true
      Principal: "*"

Outputs:
  CloudFrontUrl:
    Description: >
      CloudFront distribution URL. Use this as the Access URL when registering
      the app in Amazon Connect Integrations, and update the Okta OIDC app
      sign-in redirect URI to this value.
    Value: !Sub https://${CloudFrontDistribution.DomainName}
  FunctionUrl:
    Description: Set REACT_APP_API_BASE_URL to this Lambda Function URL.
    Value: !GetAtt RoutingProfileFunctionUrl.FunctionUrl
  FunctionName:
    Description: Lambda function name.
    Value: !Ref RoutingProfileFunction
  FrontendBucketName:
    Description: S3 bucket name for uploading the React frontend build.
    Value: !Ref FrontendBucket
